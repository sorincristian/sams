import axios from "axios";

async function run() {
  try {
    const auth = { headers: { Authorization: "Bearer " } }; // Wait, I don't have a token. But I can bypass auth locally.
  } catch(e) {
  }
}
